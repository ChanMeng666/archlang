---
paths:
  - ".github/workflows/**"
  - "package.json"
  - "CHANGELOG.md"
  - "packages/mcp/**"
  - "editors/vscode/**"
---

# Release, CI and the published packages

Read `docs/agents/project-status.md` (MCP shim pack-time law, VS Code extension's six durable
mechanics, the registry race) and the release entries of `docs/agents/iron-laws.md` plus the
`(Releasing)`/`(MCP registry)` entries of `docs/agents/gotchas.md`. Before a tag, run
`/release-check`. CI job map: `docs/agents/commands.md`.
